# API base URL. Leave as /api to use the Vite dev proxy (docker compose up
# runs the API on http://localhost:3001). Set an absolute URL for production.
VITE_API_URL=/api

# Feature Flags
VITE_ENABLE_MULTI_LEAGUE=true
